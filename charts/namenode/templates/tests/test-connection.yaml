apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "namenode.fullname" . }}-test-connection"
  labels:
    {{- include "namenode.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "namenode.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
