apiVersion: v1
kind: Service
metadata:
  name: {{ include "namenode.fullname" . }}
  labels:
    {{- include "namenode.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "namenode.selectorLabels" . | nindent 4 }}
