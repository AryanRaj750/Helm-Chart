apiVersion: v1
kind: Service
metadata:
  name: {{ include "datanode.fullname" . }}
  labels:
    {{- include "datanode.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "datanode.selectorLabels" . | nindent 4 }}
